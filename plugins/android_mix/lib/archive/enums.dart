enum ZipCompressLevel { fastest, fast, normal, maximum, ultra }
enum ZipCompressMethod { store, deflate, aes }
enum ZipEncryptionMethod { aes, standard, strong }
enum CompressionType { bzip2, gzip, pack200, xz }
enum ArchiveFormat { sevenz, tar, jar, ar, dump, cpio }
